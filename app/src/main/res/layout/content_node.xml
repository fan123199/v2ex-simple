<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">


        <com.android.volley.toolbox.NetworkImageView
            android:id="@+id/ivNodeImage"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center_horizontal"
            android:contentDescription="@string/node_image"
            android:layout_margin="12dp" />

        <TextView
            android:id="@+id/tvNodeName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="Large Text"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:layout_alignTop="@+id/ivNodeImage"
            android:layout_toEndOf="@+id/ivNodeImage"
            android:layout_alignBottom="@+id/ivNodeImage"
            android:layout_toRightOf="@+id/ivNodeImage"
            android:gravity="center_vertical" />

    <TextView
        android:id="@+id/tvNodeHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/content"
        android:layout_below="@+id/ivNodeImage"
        android:layout_alignLeft="@+id/ivNodeImage"
        android:layout_alignStart="@+id/ivNodeImage" />

</RelativeLayout>